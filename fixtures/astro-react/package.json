{
  "name": "@fixtures/astro-react",
  "private": true,
  "type": "module",
  "description": "Astro 7 fixture with React 19 islands (@astrojs/react) on the Node adapter (standalone, output: server).",
  "scripts": {
    "build": "astro build",
    "start": "node ./dist/server/entry.mjs",
    "dev": "astro dev --ignore-lock --port ${PORT:-4321}",
    "hydration-proof": "hydration-proof test"
  },
  "dependencies": {
    "@astrojs/node": "^11.1.6",
    "@astrojs/react": "^6.0.6",
    "astro": "^7.3.3",
    "react": "19.3.0",
    "react-dom": "19.3.0"
  },
  "devDependencies": {
    "hydration-proof": "workspace:*"
  }
}
